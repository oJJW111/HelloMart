<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hellomart.dao.QABoardDAO">

	<select id="listQABoard" resultType="QABoard">
		SELECT * FROM QABoard ORDER BY 1 DESC
	</select>
	
	<insert id="insertQABoard">
		INSERT INTO
		
		QABoard (subject, content, id, reRef,
		reLev, reSeq, count)
		
		VALUES (#{subject}, #{content}, #{id}, #{reRef},
		#{reLev}, #{reSeq}, #{count});
	</insert>
	
	<update id="viewCount">
		UPDATE QABoard SET count=count+1 WHERE idx=#{idx}
	</update>
	
	<select id="viewQABoard" resultType="QABoard">
		
		SELECT * FROM QABoard WHERE idx = #{idx}
	</select>
	
	<delete id="deleteQABoard">
		DELETE FROM QABoard WHERE idx = #{idx}
	</delete>
	
	<select id="getCount">
		SELECT COUNT(*) FROM QABaord
	</select>
	
	
	<!-- 답변글로 인하여 기존에 답변글들의 레벨을 1씩 증가하는 쿼리 -->
	<update id="reReLv">
		 UPDATE QABoard SET reLev=reLev+1 WHERE reRef=#{reRef} AND reLev > #{reLev}
	</update>
	
	<!-- 답글 추가 -->
	<insert id="reWrite">
		INSERT INTO 
		
		QABoard (subject, content, id, reRef,
		reLev, reSeq, count)
		
		VALUES (#{subject}, #{content}, #{id}, #{reRef},
		#{reLev}+1, #{reSeq}+1, #{count});
	
	</insert>
	
</mapper>